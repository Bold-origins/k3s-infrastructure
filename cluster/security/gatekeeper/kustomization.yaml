apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # First install Gatekeeper
  - base/helmrelease.yaml
  # Wait for CRDs
  - wait.yaml
  # Apply templates first
  - policies/templates/
  # Wait for templates to be established
  - wait-for-templates.yaml
  # Then apply constraints
  - policies/constraints/ 